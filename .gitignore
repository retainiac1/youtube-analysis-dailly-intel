# Secrets — never commit
.env
.env.*

# SQLite database + WAL/SHM sidecars
swipefile.db
swipefile.db-wal
swipefile.db-shm

# Pipeline resume state
state.json

# Python / virtualenv
.venv/
__pycache__/
*.pyc

# Editor / OS
.idea/
.DS_Store
